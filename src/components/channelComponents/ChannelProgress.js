import React from "react";

const ChannelProgress = ({ backGroundColor }) => {
  return (
    <div
      style={{ background: backGroundColor }}
      className="channel-progress-container"
    ></div>
  );
};

export default ChannelProgress;
